import { ReactElement } from "react";
import Clock from "./components/Clock";

const App = (): ReactElement => {
  return (
    <div className="App">
      <Clock />
    </div>
  );
};

export default App;
